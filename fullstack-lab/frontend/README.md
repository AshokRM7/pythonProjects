# frontend

The React app for Fullstack Lab, scaffolded with Vite.

Start reading at `src/App.jsx` — every part is commented.
Concepts: `../docs/03-react-frontend.md` · Run instructions: `../README.md`.

```powershell
npm install    # once, to recreate node_modules
npm run dev    # start the dev server on http://localhost:5173
```
